name = "geniusunleashed"
compatibility_date = "2024-09-23"
pages_build_output_dir = "svelte-app/dist"

# ─── Cloudflare Pages build configuration ─────────────────
# Build command (set in Cloudflare dashboard):
#   cd svelte-app && npm install && npm run build
# Output directory: svelte-app/dist
# Root directory: (leave empty — runs from repo root)
