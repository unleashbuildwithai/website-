name = "geniusunleashed"
compatibility_date = "2024-09-23"

# ─── Cloudflare Pages build configuration ─────────────────
# Build command: cd svelte-app && npm install && npm run build
# Output directory: svelte-app/dist
# Root directory: (leave empty — runs from repo root)
#
# Or for manual deploy: wrangler pages deploy svelte-app/dist
